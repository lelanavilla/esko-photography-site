// const express= require('express');
// const loginsRouter = express.Router();

// const LoginsModel = require('../../models/logins.js');

// loginsRouter.get("/", (req, res)=>{
//     LoginsModel.find(req.query, (err, foundLogins)=>{
//         if(err) return res.send(err);
// res.status(200).send(foundLogins);
// })
// })
// module.exports = loginsRouter;